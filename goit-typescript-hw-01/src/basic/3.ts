//literal

function choose(variable: 'disable' | 'enable'){
    if(variable === "disable") {
        console.log('disable');
    } else if(variable === "enable") {
        console.log('enable');
    };
};
choose("disable")

